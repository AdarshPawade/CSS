#Include<stdio.h>
#include<conio.h>
main()
{
    int n,i,k;
    i =1;
    printf("\n Enter a number upto 10:");
    scanf("%d",&n);
    if(n<=10)
    {
        while(i<=n)
        {
            printf("\t%d",i);
            i++
        }
        printf("\n\n");
        do
        {
            printf("\t%d",i);
            i++
        }while(i<=n);
    }
    else
    printf("\n Invalid number ");
printf("\n Enter a number 1 to 3 :");
switch(k);
{
    case 1:
    printf("One");
    break;
    case 2:
    printf("Two");
    break;
    case 3:
    printf("Three");
    break;
    default;
    printf("Invalid");
}catch();

}
        }
        }
    }
}